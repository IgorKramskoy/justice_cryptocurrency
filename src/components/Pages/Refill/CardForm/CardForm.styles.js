import { styled } from '@mui/material';

export const FormCardStyled = styled('form')(() => ({
  width: '70%',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'start',
  gap: '30px',
  marginTop: '30px',
}))