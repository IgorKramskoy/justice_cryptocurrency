import {
  styled,
  Typography
} from '@mui/material';

export const Label = styled(Typography)(() => ({
  color: '#8C939D',
  textAlign: 'start',
  fontSize: '14px',
}))
export const Info = styled(Typography)(() => ({
  color: '#8C939D',
  textAlign: 'start',
  fontSize: '12px',
}))